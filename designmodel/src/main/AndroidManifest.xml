<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fupengpeng.designmodel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".strategy.learning.use.StrategyPatternActivity" />
        <activity android:name=".strategy.instance.use.BuyGoodsActivity" />
        <activity android:name=".proxy.learning.use.ProxyActivity" />
        <activity android:name=".singleton.learning.SingletonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".multition.MultitionActivity" />
        <activity android:name=".factorymethod.FactirtMethodActivity" />
        <activity android:name=".factorymethod.learning.FactoryMethodActivity"></activity>
    </application>

</manifest>